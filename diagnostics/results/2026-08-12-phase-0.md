# Phase 0 results

Recorded against unmodified BaseOS 1.1.0. Fill each section in as it is run;
an unanswered section is a section that blocks the phase that depends on it.

| Test | Device | Answer | Blocks |
|---|---|---|---|
| T0 does BaseOS boot on an RG SP | RG SP | PASS | the whole project |
| T1 what the board publishes | both | | the poweroff primitive |
| T2 how the lid reports itself | both | | slot's input decoding |
| T3 is "off" actually off | RG35XX SP first | | the power design |
| T4 is "sleep" actually sleep | RG35XX SP first | | the lid design |
| T5 can the lid wake from mem | both | | whether the DTB gets patched |
| T6 what U-Boot does before the kernel | both | | boot-time rung E |

## T0 — RG SP cold boot

Card written: BaseOS 1.1.0
Result: PASS. Kernel 4.9.170, all five /run/boot-* markers present, adb up.
Notes: Serial ac001089c8958701d12.

## T1 — enumeration

`ags-probe.txt` sections of interest. Record the answer, not the dump:

- Does the cmdline set `console=`? Yes, `console=ttyS0,115200`
- Beyond `os_sleep`, does the AXP2202 publish anything that could force the PMIC off? No attribute found capable of forcing PMIC power off.
- Which input node is the hall sensor, and does it advertise `SW_LID`? Battery is `axp2202-battery`, `charge_full` = 3500000 uAh (3500 mAh). Hall sensor is on the PMIC as `hallkey` sysfs attribute, not as evdev input node. No `SW_LID`.
- Is `wakealarm` present and writable? `os_sleep` attribute reads empty.

## T2 — lid events

Method: `SLOT_TRACE_INPUT=1`, ten close/open cycles, read `input-trace.log`.
Codes observed: Every input node reports `sw = 0`. There is no `SW_LID` on this board.
Does slot's `EV_SW`/`SW_LID` decoding match: The lid is the PMIC's `hallkey` sysfs attribute, not an evdev switch.

## T3 — is "off" actually off

`python3 diagnostics/power-sampler/ags-analyse.py ags-power.csv`

power-cycle gap duration:
average current:
verdict:

## T4 — is "sleep" actually sleep

suspend gap duration:
average current:
Super Standby applied (`os_sleep` = 16):

## T5 — lid as a wake source

`echo enabled > .../power/wakeup` accepted: Hall switch is IRQ 120 (`sunxi_pio_edge 103 Edge  hall switch`). Already registered as the power-key driver's wake IRQ per dmesg: `axp20x_pek_probe: wakup_irq=120`.
Lid opened the device from `mem`:

## T6 — U-Boot

`bootcmd`: 
Anything it does that is not required: `console=ttyS0,115200 loglevel=4 earlyprintk=sunxi-uart,0x05000000 initcall_debug=0`. U-Boot env templates as `console=${console} loglevel=${loglevel} earlyprintk=${earlyprintk}`, with `bootdelay=0`.
