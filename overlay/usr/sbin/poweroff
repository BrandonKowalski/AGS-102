#!/bin/sh
# Compatibility shim. slot's Power::poweroff() reaches Command::new("poweroff"),
# an execvp against init's PATH, so the fix has to live under the bare name.
# The logic is in ags-poweroff.
exec "${AGS_SBIN:-/usr/sbin}/ags-poweroff" "$@"
